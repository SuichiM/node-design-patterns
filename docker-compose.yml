version: "3"
services:
  web:
    build: .
    volumes:
      - .:/home/node/app
      - /home/node/app/node_modules
    ports:
      - 8888:8888
      - 8889:80
    environment:
        - COMMAND=${COMMAND}
        - PORT=${BACKEND_PORT}