version: '3'
services:
    web:
        build: .
        volumes:
            - .:/home/node/app
            - /home/node/app/node_modules
        expose:
            - 8888
        ports:
            - 8888:8888

